[project]
name = "parity-tool-scan-flow"
version = "0.1.0"
description = "Package a Python local lab for scan analysis with node-edge fixtures, cycle and reachability reports, and documented operating limits."
requires-python = ">=3.11"
